import test from "node:test";import assert from "node:assert/strict";import {Nodra} from "./index.ts";
test("protect exposes intent/result helpers",()=>{const n=new Nodra({baseUrl:"https://example.test",workspaceId:"workspace",signingSecret:"12345678901234567890123456789012"});const a=n.protect({id:"support"});assert.equal(typeof a.intent,"function");assert.equal(typeof a.result,"function");assert.equal(typeof a.record,"function")});
