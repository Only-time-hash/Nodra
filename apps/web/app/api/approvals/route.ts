import {NextResponse} from "next/server";import {getWorkspaceContext} from "../../../lib/persistence";
export async function GET(){const ctx=await getWorkspaceContext();if(!ctx)return NextResponse.json({error:"authentication_or_workspace_required"},{status:401});const {data,error}=await ctx.supabase.from("security_events").select("id,event_type,action,decision,occurred_at,sequence_no,agent_id,payload,agents!security_events_agent_id_fkey(external_id,name)").eq("workspace_id",ctx.workspaceId).eq("decision","require_approval").order("sequence_no",{ascending:false}).limit(100);if(error)return NextResponse.json({error:"approval_query_failed"},{status:500});return NextResponse.json({approvals:data??[]})}